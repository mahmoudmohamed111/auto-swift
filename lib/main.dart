import 'package:flutter/material.dart';

void main() {
  runApp(Auto_swift_app());
}

class Auto_swift_app extends StatelessWidget {
  const Auto_swift_app({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(home: Scaffold());
  }
}
